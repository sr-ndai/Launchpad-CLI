# Task Prompt: 9.6 - Logs and utility command redesign

## Objective

Move the human-readable `launchpad logs`, `launchpad ls`, `launchpad cancel`,
and `launchpad cleanup` surfaces onto the corrected Phase 9 display hierarchy
while preserving existing command behavior, selector semantics, and
machine-readable paths.

## Context

- The active coordination branch is `phase/09-cli-visual-overhaul`.
- Task `9.5` now corrects the shared hierarchy system and retrofits the
  already-migrated Phase 9 surfaces. This task must build on that corrected
  system rather than on the older dim-heavy text treatment.
- The remaining operator-facing commands still carry older panel-heavy or
  command-local presentation patterns. They need to be unified after the
  hierarchy revision settles.
- The detailed design references remain
  `.ai/plans/LAUNCHPAD_UI_REDESIGN_PLAN.md` sections `3.6` to `3.9` and
  `.ai/plans/LAUNCHPAD_UI_REVISION.md`.
- The logs redesign should stay conservative: once raw log content starts
  streaming or following, the chrome should mostly disappear so the user reads
  the log, not the wrapper.

## In Scope

- Redesign human-readable `launchpad logs` header/output so snapshot and follow
  flows use a minimal header and keep raw log reading unobstructed.
- Redesign `launchpad cancel` confirmation and result output around restrained
  confirmation copy, inline status changes, and clearer destructive-flow
  feedback.
- Redesign `launchpad ls` populated and empty states around the Phase 9
  borderless listing grammar and corrected text hierarchy.
- Redesign `launchpad cleanup` candidate listing, selection prompt, and result
  output around the restrained utility-command style.
- Keep existing selectors, aliases, JSON output, destructive semantics,
  `--yes`, `--follow`, and other command behavior unchanged.
- Update or add regression coverage for the redesigned human-readable logs and
  utility-command surfaces.

## Out of Scope

- Redesigning `download`, `submit`, `status`, `config`, or `doctor`
- Changing log resolution behavior, cleanup selection semantics, remote file
  traversal, cancellation logic, or selector interpretation beyond display-only
  adjustments
- Changing machine-readable outputs or existing confirmation defaults
- Docs/README refresh beyond inline comments or helper notes needed to support
  the implementation

## Deliverables

- Phase 9 human-readable `logs` header/streaming surface
- Phase 9 human-readable `ls`, `cancel`, and `cleanup` output
- Regression coverage for the updated logs and utility-command surfaces

## Branch

`task/9.6-logs-and-utility-command-redesign`

## Verification

- `uv run pytest tests/test_logs.py tests/test_ls.py tests/test_cancel.py tests/test_cleanup.py tests/test_cli.py`
- `uv run pytest`

## References

- `.ai/plan.md`
- `.ai/plans/09-cli-visual-overhaul.md`
- `.ai/plans/LAUNCHPAD_UI_REDESIGN_PLAN.md`
- `.ai/plans/LAUNCHPAD_UI_REVISION.md`
- `src/launchpad_cli/display.py`
- `src/launchpad_cli/cli/logs.py`
- `src/launchpad_cli/cli/ls.py`
- `src/launchpad_cli/cli/cancel.py`
- `src/launchpad_cli/cli/cleanup.py`
- `tests/test_logs.py`
- `tests/test_ls.py`
- `tests/test_cancel.py`
- `tests/test_cleanup.py`
