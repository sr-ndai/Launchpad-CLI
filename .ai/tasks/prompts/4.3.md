# Task Prompt: 4.3 - Remote listing and cleanup commands

## Objective

Implement the remaining Phase 4 operator commands for remote file listing and
remote job-directory cleanup.

## Context

- Phase 4 closes the loop after download by giving engineers direct visibility
  into remote result directories and a safe cleanup path from the local CLI.
- `.ai/plan.md` sections 3.6, 3.9, 4.2, 8, 12, and 15 describe the intended
  operator command behavior, testing expectations, and documentation standard.
- This task depends on `4.2` being complete and merged.

## In Scope

- Replace the placeholder `launchpad ls` command with the documented remote
  listing behavior, including default-path handling, long-format output, and
  glob or path selection.
- Replace the placeholder `launchpad cleanup` command with the documented
  remote directory management flow, including confirmation behavior and
  age-filtered cleanup.
- Add focused tests for both commands using fakes for remote effects.
- Update documentation for both operator commands.

## Out of Scope

- `launchpad download` orchestration or download-path verification.
- Phase 5 performance tuning, typo suggestions, or broader JSON rollout.

## Deliverables

- `src/launchpad_cli/cli/ls.py`
- `src/launchpad_cli/cli/cleanup.py`
- Associated tests and documentation updates

## Branch

`task/4.3-remote-ls-and-cleanup`

## Verification

- `uv run pytest tests/test_ls.py tests/test_cleanup.py`
- `uv run launchpad ls --help`
- `uv run launchpad cleanup --help`
- `uv run pytest`

## References

- `.ai/plan.md`
- `.ai/plans/04-download-cleanup.md`
- `.ai/tasks/queue.md`
