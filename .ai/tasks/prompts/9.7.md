# Task Prompt: 9.7 - Help, docs, and regression hardening

## Objective

Finish Phase 9 by refreshing help/examples/docs and locking the corrected UI
hierarchy plus the redesigned command surfaces behind focused regression
coverage.

## Context

- The active coordination branch is `phase/09-cli-visual-overhaul`.
- Tasks `9.1` through `9.6` carry the full human-readable CLI overhaul and the
  hierarchy correction driven by expert feedback in
  `.ai/plans/LAUNCHPAD_UI_REVISION.md`.
- The remaining work is documentation consistency and regression hardening so
  the final Phase 9 branch no longer references stale Phase 7 terms, dim-heavy
  copy, or superseded panel assumptions.
- Keep the existing test philosophy: semantic assertions plus focused helper
  tests, not brittle full-output snapshots.

## In Scope

- Refresh help/examples and CLI-facing docs to match the final Phase 9 output
  and hierarchy rules.
- Remove stale test expectations that still reference old titles, panel copy,
  or outdated runtime wording.
- Add or tighten shared display helper coverage where needed to protect the
  hierarchy revision and the remaining command surfaces.
- Re-run the relevant Phase 9 command suites and full pytest coverage to
  confirm the final UI branch is internally consistent.

## Out of Scope

- New visual redesign work beyond the already-approved Phase 9 scope
- Behavior changes to commands, flags, machine-readable outputs, or selector
  semantics
- Any new product features or solver/transfer/scheduler logic changes

## Deliverables

- Updated help/examples/docs aligned with final Phase 9 output
- Hardened regression coverage for the corrected hierarchy and remaining CLI
  surfaces
- A phase branch ready for final Phase 9 verification and PR creation

## Branch

`task/9.7-help-docs-and-regression-hardening`

## Verification

- `uv run pytest tests/test_cli.py tests/test_display.py tests/test_config.py tests/test_operator_commands.py tests/test_submit.py tests/test_status.py tests/test_download.py tests/test_logs.py tests/test_ls.py tests/test_cancel.py tests/test_cleanup.py`
- `uv run pytest`

## References

- `.ai/plan.md`
- `.ai/plans/09-cli-visual-overhaul.md`
- `.ai/plans/LAUNCHPAD_UI_REDESIGN_PLAN.md`
- `.ai/plans/LAUNCHPAD_UI_REVISION.md`
- `src/launchpad_cli/display.py`
- `tests/`
