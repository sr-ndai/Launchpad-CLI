# Decision Log

> Append-only. The Coordinator logs significant decisions here.
> **Never delete or modify existing entries.**

| Date | Decision | Rationale | Impact |
|------|----------|-----------|--------|
| 2026-03-12 | Create a local `main` branch before initializing the workflow. | The repository arrived with `master` only, but the installed `.ai` system and branch model require `main` as the default baseline. | Coordination and task branches now follow the documented `main -> phase -> task` structure without rewriting existing history. |
| 2026-03-12 | Split Phase 1 into four focused tasks and assign scaffolding first. | The foundation phase spans packaging, configuration, transport, and operator UX; smaller prompts reduce review scope and keep Builder work sequentially actionable. | The queue now contains tasks `1.1` to `1.4`, with task `1.1` active and the remaining tasks staged behind explicit dependencies. |
| 2026-03-12 | Defer Phase 1 PR creation until the remote branch baseline is aligned. | The local workflow now uses `main`, but the remote still only exposes `origin/master`, and no local PR CLI is installed to automate creation. | Phase 1 is complete locally on `phase/01-foundation`, but the PR step remains a coordination follow-up rather than part of this review pass. |
| 2026-03-12 | Start Phase 2 from refreshed `main` after the Phase 1 PR merge landed. | `origin/main` now contains the merged Phase 1 history, so Phase 2 can branch from the human-approved baseline instead of continuing on the completed phase branch. | The active coordination branch moves to `phase/02-submission-pipeline`, and Phase 1 remains closed rather than accumulating new planning work. |
| 2026-03-12 | Split Phase 2 into three sequential tasks and assign solver groundwork first. | Submission work spans solver abstraction, remote submit primitives, and CLI orchestration; separating them keeps reviews narrow and prevents the submit command from being built on unstable interfaces. | The queue now stages tasks `2.1` to `2.3`, with `2.1` active and later tasks gated on explicit dependencies. |
| 2026-03-12 | Close Phase 2 locally and defer PR creation until GitHub credentials are available in the environment. | All Phase 2 tasks are accepted and merged, but this shell has neither `gh` nor a GitHub API token, so automated PR creation cannot be completed from the CLI. | `phase/02-submission-pipeline` is ready to merge into `main`, and PR creation is the only remaining step before human review. |
| 2026-03-12 | Use git's stored GitHub credentials to create the Phase 2 PR through the GitHub API when `gh` is unavailable. | The machine's `osxkeychain` credential helper already held valid GitHub auth, so PR creation did not actually require installing `gh` or exposing a separate token in the environment. | Phase 2 PR `#2` is open without changing repository history or local branch structure, and future coordination can reuse the same fallback when needed. |
| 2026-03-12 | Start Phase 3 from refreshed `main` after the Phase 2 PR merge landed. | `origin/main` now contains the merged Phase 2 history, so monitoring and logs work can branch from the human-approved baseline instead of accumulating on the completed phase branch. | The active coordination branch moves to `phase/03-monitoring-logs`, and Phase 2 remains closed rather than taking new planning work. |
| 2026-03-12 | Split Phase 3 into three sequential tasks and assign SLURM status groundwork first. | Monitoring work spans scheduler parsing, status UI, and operator commands; separating them keeps reviews narrow and lets the command layer build on stable status/query contracts. | The queue now stages tasks `3.1` to `3.3`, with `3.1` active and later tasks gated on explicit dependencies. |
| 2026-03-13 | Reuse git's stored GitHub credentials to create the Phase 3 PR through the GitHub API when `gh` is unavailable. | The Phase 2 fallback remained valid in this environment: `gh` is still absent, but the machine's git credential helper can authenticate direct GitHub API requests. | Phase 3 PR `#3` is open without changing the branch model or installing additional tooling. |
| 2026-03-13 | Start Phase 4 from refreshed `main` after the Phase 3 PR merge landed. | `origin/main` now contains the merged Phase 3 history, so download and cleanup work can branch from the human-approved baseline instead of accumulating on the completed Phase 3 branch. | The active coordination branch moves to `phase/04-download-cleanup`, and Phase 3 remains closed rather than taking new planning work. |
| 2026-03-13 | Split Phase 4 into three sequential tasks and assign download primitives first. | Phase 4 spans core filesystem/archive primitives, download orchestration, and remote operator commands; separating them keeps reviews narrow and lets the CLI layer build on stable helpers. | The queue now stages tasks `4.1` to `4.3`, with `4.1` active and later tasks gated on explicit dependencies. |
| 2026-03-13 | Reuse git's stored GitHub credentials to create the Phase 4 PR through the GitHub API when `gh` is unavailable. | The Phase 2 and Phase 3 fallback remained valid in this environment: `gh` is still absent, but the machine's git credential helper can authenticate direct GitHub API requests. | Phase 4 PR `#4` is open without changing the branch model or installing additional tooling. |
| 2026-03-13 | Start Phase 5 from refreshed `main` after the Phase 4 PR merge landed. | `origin/main` now contains the merged Phase 4 history, so performance and polish work can branch from the human-approved baseline instead of accumulating on the completed Phase 4 branch. | The active coordination branch moves to `phase/05-performance-polish`, and Phase 4 remains closed rather than taking new planning work. |
| 2026-03-13 | Split Phase 5 into three sequential tasks and assign benchmark work first. | Phase 5 spans benchmark-driven transfer strategy selection, runtime transfer hardening, and cross-cutting CLI polish; separating them avoids implementing unvalidated transfer ideas and keeps the later polish work grounded in the settled behavior. | The queue now stages tasks `5.1` to `5.3`, with `5.1` active and later tasks gated on explicit dependencies. |
| 2026-03-13 | Defer the real-cluster transfer benchmark and keep single-stream as the provisional Phase 5 baseline. | The current workstation cannot resolve Launchpad cluster config, SSH credentials, or the shared `/shared/config/launchpad.toml`, so the roadmap's real-cluster benchmark is not actionable yet. | Task `5.1` now produces the deferred-validation plan and provisional recommendation, and task `5.2` will harden the current single-stream path instead of introducing speculative parallel-transfer behavior. |
| 2026-03-13 | Replace the deferred single-stream fallback with a hybrid `single-file` plus `multi-file` Phase 5 transfer design. | The human explicitly wants both fast single-payload transfer and many-file transfer with shared stream controls, and established tools already prove that these should be treated as separate but coordinated transfer modes. | Task `5.1` now chooses the architecture from external precedent, task `5.2` will implement the hybrid engine and CLI surface, and real-cluster benchmarking becomes later tuning rather than the design gate. |
| 2026-03-13 | Repair the roadmap and routing state from Phase 5 to a post-Phase 6 baseline before opening new work. | `main` already contains the merged `phase/06-documentation` history, but `.ai/state/current.md`, `.ai/tasks/queue.md`, and the roadmap summary still stopped at the Phase 5 PR handoff. | Coordination now resumes from an accurate baseline on `main`, and the next active product phase is Phase 7 rather than a stale Phase 5 continuation. |
| 2026-03-13 | Split Phase 7 into four sequential tasks and assign the shared design-system groundwork first. | The requested CLI beautification spans shared theming/help, guided onboarding, primary workflow polish, and secondary-command consistency; landing the design language first prevents repeated per-command rework. | The queue now contains tasks `7.1` to `7.4`, with `7.1` active and the remaining work staged behind explicit dependencies. |
| 2026-03-14 | Reframe Phase 7 around three distinct surfaces: welcome screen, root help reference, and command/runtime UX. | First-pass review feedback showed that branded `--help` output creates visual noise and puts personality on the wrong surface; the no-argument path should be the landing screen, while `--help` stays dry and scannable. | Task `7.1` now requires revision, and tasks `7.2` to `7.4` inherit the new rule that branding lives on welcome or onboarding moments while runtime flows carry most of the CLI personality. |
| 2026-03-14 | Start Phase 8 from refreshed `main` after the Phase 7 merge landed. | `origin/main` now contains the accepted Phase 7 runtime UX, so the next planning cycle should branch from the human-approved baseline instead of from the stale Phase 7 coordination branch. | The active coordination branch moves to `phase/08-task-references-and-solver-aware-logs`, and Phase 7 remains closed rather than accumulating new follow-up planning work. |
| 2026-03-14 | Split Phase 8 into three sequential tasks and assign manifest-first groundwork first. | Task references, solver-aware logs, and interactive selection all depend on a stable submitted metadata contract; landing the manifest and task-reference model first prevents downstream selector and picker rework. | The queue now contains tasks `8.1` to `8.3`, with `8.1` active and the remaining tasks gated behind explicit dependencies. |
| 2026-03-14 | Keep solver identity out of the SLURM comment and use the remote manifest as the only new metadata authority. | The current SLURM comment is already treated as the remote job-directory pointer by status and cleanup flows, so overloading it with structured solver metadata would create unnecessary compatibility risk. | New jobs will store solver and solver-log metadata in `launchpad-manifest.json`, while legacy jobs keep the existing comment-only behavior and use best-effort fallback where needed. |
| 2026-03-14 | Reopen Phase 8 with task `8.4` for cluster-access diagnostics and the Windows SSH path. | Field debugging showed two regressions outside the original manifest/log scope: `doctor` probes remote binaries under a forced `sh -lc` wrapper which can misreport the remote exec environment, and Windows `launchpad ssh` crashes when AsyncSSH redirects local stdio through the Proactor loop. | Phase 8 PR `#9` must absorb one follow-up task before human review is complete, and the active coordination state now points Builder work at `task/8.4-cluster-access-diagnostics`. |
| 2026-03-14 | Reopen Phase 8 again with task `8.5` for SLURM login-shell execution. | Field debugging after `8.4` showed that scheduler commands such as `squeue` and `sacct` are available on the head node only after login-shell initialization, so Launchpad still fails on status/logs while invoking them directly in a non-login exec session. | Phase 8 remains active locally, the queue now stages `8.5`, and the next Builder task must update the shared SLURM execution path plus the aligned doctor diagnostics before PR `#9` is ready for final human review. |
| 2026-03-14 | Reopen Phase 8 again with task `8.6` for a configurable remote workspace root. | Field debugging after `8.5` showed that Launchpad still hardcodes its writable remote workspace as `<shared_root>/<ssh.username>`, but the reported cluster uses a different shared writable directory such as `/shared/launchpad`. | Phase 8 remains active locally, the queue now stages `8.6`, and the next Builder task must add a dedicated workspace-root config field plus shared resolver updates across submit, doctor, ls, and cleanup before PR `#10` is ready for human review. |
| 2026-03-14 | Reopen Phase 8 again with task `8.7` for syntax-highlighted `config show` output. | Field feedback after `8.6` showed that `launchpad config show` still emits plain TOML text in its primary human-readable path, which is inconsistent with the rest of the Rich-based CLI presentation. | Phase 8 remains active locally, the queue now stages `8.7`, and the next Builder task must update the `config show` presentation path while preserving the existing `--json` and `--docs` outputs before PR `#10` is ready for human review. |
| 2026-03-14 | Reopen Phase 8 again with task `8.8` for shared SLURM parser hardening. | Field debugging after `8.7` showed that Launchpad can crash in `status` when cluster-specific SLURM JSON exposes a host-style string in a field Launchpad currently reuses as an integer source. | Phase 8 remains active locally, the queue now stages `8.8`, and the next Builder task must harden the shared SLURM parser so `status` and the other scheduler-consuming commands tolerate the reported payload shape before PR `#11` is ready for human review. |
| 2026-03-14 | Start Phase 9 from refreshed `main` after the Phase 8 PR merge landed. | `origin/main` now contains the merged Phase 8 follow-ups from PR `#12`, so new coordination work should branch from the human-approved baseline instead of the stale Phase 8 coordination branch. | The active coordination branch moves to `phase/09-cli-visual-overhaul`, and Phase 8 remains closed rather than taking more planning work. |
| 2026-03-14 | Scope Phase 9 as a full CLI visual overhaul with welcome-only branding and a sectioned human `config show`. | The redesign brief intentionally supersedes the Phase 7 panel-heavy runtime surfaces, but the chosen direction keeps branding only on bare `launchpad` and moves human-readable `config show` to a grouped summary without changing machine interfaces. | Tasks `9.1` through `9.6` now cover all human-facing CLI surfaces except the interactive `launchpad ssh` session body, while preserving flags, exit codes, selectors, and JSON payloads. |
| 2026-03-14 | Reopen task `9.2` and pause `9.3` after finding a committed `doctor` runtime regression in review follow-up. | The accepted `9.2` branch references `sys.version_info` without importing `sys`, so `launchpad doctor` crashes before any diagnostics render; continuing into `9.3` would stack new work on top of a broken Phase 9 baseline. | Task `9.2` is back to `revision-needed` with a dated prompt revision, `9.3` returns to `ready`, and the next Builder pass must repair `doctor` plus add regression coverage before the submit/status redesign resumes. |
| 2026-03-14 | Carry forward stronger key/value label contrast as a Phase 9 runtime rule starting with task `9.3`. | Review feedback on the `9.2` surfaces showed that faint grey keys against brighter values makes labels read like footnotes instead of navigational anchors. | The `9.3` prompt now explicitly requires stronger key emphasis, and later Phase 9 command redesigns should follow the same contrast rule unless a future design decision replaces the key/value treatment entirely. |
| 2026-03-15 | Insert a dedicated Phase 9 hierarchy-correction pass before the remaining command migrations. | Expert feedback in `.ai/plans/LAUNCHPAD_UI_REVISION.md` showed that the first redesign pass fixed the CLI structure but still over-compressed labels, values, hints, and suggestions into the same dim tier, so continuing directly into logs/utilities would bake the weak hierarchy deeper into the phase. | Active task `9.5` is revised on its task branch to correct the shared display hierarchy and retrofit the already-migrated surfaces, logs/utilities move to `9.6`, help/docs/regression hardening moves to `9.7`, and the revision brief becomes a tracked Phase 9 design reference. |
| 2026-03-15 | Open Phase 10 as a focused follow-up for Nastran environment exports and live running-job metrics. | The change was prototyped locally after Phase 9 but intentionally restored before the Phase 9 merge when the Coordinator role boundary was reasserted; the human has now explicitly asked for that work to be landed with a proper `.ai` trail. | Coordination resumes on `phase/10-cluster-env-and-live-metrics`, task `10.1` becomes the active implementation branch, and the roadmap now tracks the deferred cluster-facing work explicitly instead of leaving it as an out-of-band patch. |
