# Builder Session - 8.1 - 2026-03-14 1353

## Objective

Establish the Phase 8 submitted-metadata contract by adding the remote job
manifest, stable task references, and the first submit/status surfaces that
show those references.

## Outcome

Outcome: READY_FOR_REVIEW

## What Was Done

- Added typed solver log-catalog config under `solvers.<solver>.logs` and
  threaded the resolved catalog through the solver adapters used by submit.
- Introduced a dedicated `launchpad-manifest.json` contract with versioned
  top-level solver/log metadata plus stable per-task references.
- Updated submit planning/execution to generate task references, write the
  remote manifest for new submissions, and expose the manifest-backed data in
  additive JSON fields plus multi-input human output.
- Updated job-detail status to load the manifest when present and surface label,
  alias, and raw task ID columns for submitted array tasks.
- Expanded tests for config normalization, manifest/task-reference generation,
  manifest persistence during submit, and manifest-backed status rendering.

## Files Changed

- src/launchpad_cli/cli/status.py
- src/launchpad_cli/cli/submit.py
- src/launchpad_cli/core/config.py
- src/launchpad_cli/core/job_manifest.py
- src/launchpad_cli/core/remote_ops.py
- src/launchpad_cli/display.py
- src/launchpad_cli/solvers/ansys.py
- src/launchpad_cli/solvers/base.py
- src/launchpad_cli/solvers/nastran.py
- tests/test_config.py
- tests/test_status.py
- tests/test_submit.py

## Verification Results

- `uv run pytest tests/test_submit.py tests/test_status.py tests/test_slurm.py tests/test_config.py tests/test_cli.py` -> passed (39 passed)
- `uv run pytest` -> passed (123 passed)

## Observations

- I chose a conservative stable alias format of `tNN`, using one-based,
  zero-padded human references while preserving the raw zero-based SLURM
  `task_id` separately in the manifest and UI.
- I limited manifest-backed status enrichment to the job-detail flow for this
  task. I did not extend overview status rows or other commands to consume the
  new references yet because those behaviors belong to later Phase 8 tasks.
