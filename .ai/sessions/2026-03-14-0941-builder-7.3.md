## Objective

Apply the accepted Phase 7 shell to the primary workflow runtime surfaces for
task `7.3`: `submit`, `status`, and `download`.

## Outcome

Outcome: READY_FOR_REVIEW

## What Was Done

- Refreshed `submit --dry-run` and successful `submit` output so both surfaces
  use stronger Phase 7 hierarchy, clearer copy, and explicit next commands.
- Added cleaner live and non-live `status` headers, improved empty-state
  guidance, and next-command hints for job detail without adding decorative
  branding to watch refreshes.
- Updated `download` preview and completion output to use the shared panel
  language, improved the confirmation prompt, and refreshed the command docs
  and tests around the new workflow cues.

## Files Changed

- docs/reference/commands/download.md
- docs/reference/commands/status.md
- docs/reference/commands/submit.md
- src/launchpad_cli/cli/download.py
- src/launchpad_cli/cli/status.py
- src/launchpad_cli/display.py
- tests/test_download.py
- tests/test_status.py
- tests/test_submit.py

## Verification Results

- `uv run pytest tests/test_submit.py tests/test_status.py tests/test_download.py tests/test_cli.py` -> passed (30 passed)
- `uv run pytest` -> passed (117 passed)

## Observations

- I kept the runtime personality focused on hierarchy, success framing, and
  next-step guidance rather than adding more ASCII branding, which matches the
  Phase 7 direction that personality should live in the verbs rather than
  repeated chrome.
