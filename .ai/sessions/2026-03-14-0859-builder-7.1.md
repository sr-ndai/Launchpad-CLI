## Objective

Revise task `7.1` so Launchpad uses the corrected Phase 7 three-surface model:
bare-command welcome screen, compact root help reference, and restrained
command help.

## Outcome

Outcome: READY_FOR_REVIEW

## What Was Done

- Reworked the root CLI flow so bare `launchpad` renders a dedicated welcome
  screen while `launchpad --help` stays a dry four-panel reference card with
  the required `Get started:` epilog.
- Moved the welcome-screen composition into shared display helpers, including a
  checked-in wordmark, a compact narrow-terminal fallback, and suppression for
  `--quiet`, `--json`, `--no-color`, `NO_COLOR`, and non-TTY output.
- Updated README and the getting-started guide to describe the three help
  surfaces and adjusted CLI tests to lock the revised behavior.

## Files Changed

- README.md
- docs/guides/getting-started.md
- src/launchpad_cli/cli/__init__.py
- src/launchpad_cli/display.py
- tests/test_cli.py

## Verification Results

- `uv run pytest tests/test_cli.py` -> passed (7 passed)
- `uv run pytest` -> passed (113 passed)

## Observations

- I kept bare `launchpad` as the welcome surface even when root flags like
  `--quiet`, `--json`, or `--no-color` are present, but only the branding is
  suppressed in those modes. That matches the dated prompt revision's wording
  more closely than falling back to root help.
