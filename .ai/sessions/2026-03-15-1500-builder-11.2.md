# Builder Session - 11.2 - 2026-03-15

## Objective
Fix all places in the CLI command files that bypass or violate the Phase 9
design system: error message quality, missing progress indicators, and
`click.echo("Interrupted.")` quiet-mode leaks.

## Outcome
READY_FOR_REVIEW

## What Was Done

### Error message quality — all `cli/*.py` files

Added actionable suggestions to every bare `raise click.ClickException(message)`
that lacked a next step:

**`src/launchpad_cli/cli/submit.py`** (4 messages):
- "Input directory does not exist" → added "Run from your solver input folder or
  pass the path explicitly."
- "Input path is not a directory" → added "Pass a directory path or run from
  your solver input folder."
- "No supported {solver_key} input files were found" → added "Use --include-all
  to transfer everything, or check solver detection with --solver."
- "Extra file does not exist" → added "Check the path is relative to the input
  directory or use an absolute path."

**`src/launchpad_cli/cli/status.py`** (2 messages):
- "--watch does not support --json" → added "Use --json without --watch for a
  one-shot status snapshot."
- "No SLURM job data found for {id}" → added "Verify the job ID with:
  launchpad status"

**`src/launchpad_cli/cli/download.py`** (3 messages):
- "Download destination already exists and is not empty" → added "Use --output
  to choose a different path or delete the existing directory."
- "Task selection did not match job rows" → added "Verify task IDs with:
  launchpad status {job_id}"
- "--tasks requires at least one task reference" → added "Pass comma-separated
  task IDs or aliases."

**`src/launchpad_cli/cli/logs.py`** (3 messages):
- "accepts only one of --solver-log, --log-kind, or --err" → added "Specify
  exactly one log flag at a time."
- "--follow does not support --json" → added "Run without --follow or --json
  to get a buffered snapshot."
- "multiple task logs. Specify a TASK_REF explicitly" → added "Run launchpad
  status {job_id} to see available task IDs."

**`src/launchpad_cli/cli/cancel.py`** (1 message):
- "Task references must not be empty" → added "Provide a valid SLURM task ID
  or alias."

**`src/launchpad_cli/cli/cleanup.py`** (5 messages):
- "Cleanup only supports terminal jobs" → added "Wait for the job to finish or
  cancel it first with: launchpad cancel"
- "No remote job directory was found" → added "Verify the job ID with:
  launchpad status"
- "Cleanup target is ambiguous" → added "Verify the job ID with: launchpad
  status"
- "Cleanup selection must use numbers" → added "Enter comma-separated row
  numbers from the table above."
- "Cleanup selection is out of range" → added "Valid range is 1–{len(targets)}."
- "No SLURM job data found" → added "Verify the job ID with: launchpad status"

**`src/launchpad_cli/cli/ssh_cmd.py`** (1 message):
- "requires an interactive terminal" → added "Run this command directly in a
  terminal, not from a script or pipe."

### Missing spinners — `ls.py`, `cleanup.py`, `logs.py`

**`src/launchpad_cli/cli/ls.py`**:
- Added `build_spinner` to display imports.
- Added optional `console=None` parameter to `_run_ls()`.
- Wrapped `list_remote_directory()` call with
  `build_spinner(console, "Listing remote directory...")` when `console` is
  provided.
- Updated call site in `command()` to pass `console`.
- Tests call `_run_ls()` without `console`; the `None` default preserves
  backward compatibility with no test changes required.

**`src/launchpad_cli/cli/cleanup.py`**:
- Added `build_spinner` to display imports.
- Added optional `console=None` parameter to `_discover_cleanup_targets()`.
- Wrapped `list_remote_directory()` call with
  `build_spinner(console, "Listing remote directory...")`.
- Wrapped the `measure_remote_path()` loop with
  `build_spinner(console, "Measuring sizes...")`.
- Updated call site in `_run_cleanup()` to pass `console`.

**`src/launchpad_cli/cli/logs.py`**:
- Added `build_spinner` to display imports.
- Added optional `console=None` parameter to `_run_logs()`.
- In `_run_logs()`, wrapped the non-follow `_read_remote_log()` call with
  `build_spinner(console, "Reading log...")` when `console` is not None.
- Updated call site in `command()` to pass `console=console`.
- Tests use `fake_run_logs(**kwargs)` so no test updates were needed.

### `click.echo("Interrupted.")` → `console.print(...)` — 3 files

Replaced in all three `KeyboardInterrupt` handlers:

- `src/launchpad_cli/cli/status.py` line ~207
- `src/launchpad_cli/cli/logs.py` line ~173
- `src/launchpad_cli/cli/download.py` line ~293

Pattern applied:
```python
# BEFORE
click.echo("Interrupted.")
# AFTER
console.print("  Interrupted.", style="lp.text.tertiary")
```

### Help text completeness
Scanned all 9 CLI files — no missing `help=` strings found; no changes needed.

## Files Changed
- `src/launchpad_cli/cli/submit.py`
- `src/launchpad_cli/cli/status.py`
- `src/launchpad_cli/cli/download.py`
- `src/launchpad_cli/cli/logs.py`
- `src/launchpad_cli/cli/cancel.py`
- `src/launchpad_cli/cli/ls.py`
- `src/launchpad_cli/cli/cleanup.py`
- `src/launchpad_cli/cli/ssh_cmd.py`

## Verification Results

```
pytest -x
173 passed in 0.70s
```

## Observations
- `console=None` default on `_run_ls` and `_run_logs` kept existing tests
  passing without modification.
- For `_discover_cleanup_targets`, the spinner logic refactored the filtering
  into a `candidate_entries` list before the size-measurement loop so both
  the listing and measuring spinners could wrap coherent single operations.
- `console` was already in scope at all three `KeyboardInterrupt` sites so no
  additional plumbing was needed for the Interrupted fixes.
