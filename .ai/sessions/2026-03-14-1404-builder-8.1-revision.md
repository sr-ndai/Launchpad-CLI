# Builder Session - 8.1 - 2026-03-14 1404

## Objective

Address the coordinator review for task `8.1` by correcting the submitted
task-alias contract to the approved plain zero-padded numeric format.

## Outcome

Outcome: READY_FOR_REVIEW

## What Was Done

- Updated task-reference alias generation in
  `src/launchpad_cli/core/job_manifest.py` from the previous `tNN` format to
  the approved plain numeric format with minimum width 3 (`001`, `002`, ...).
- Updated the submit and status regression tests so they assert the approved
  alias contract consistently in rendered output, JSON payloads, and persisted
  manifest content.

## Files Changed

- src/launchpad_cli/core/job_manifest.py
- tests/test_status.py
- tests/test_submit.py

## Verification Results

- `uv run pytest tests/test_submit.py tests/test_status.py tests/test_slurm.py tests/test_config.py tests/test_cli.py` -> passed (39 passed)
- `uv run pytest` -> passed (123 passed)

## Observations

- I kept the alias numbering one-based while changing only the string format,
  which matches the review requirement to correct the contract without
  broadening scope into other selector behavior.
