## Objective

Build the guided setup and diagnostics UX for task `7.2` on top of the accepted
Phase 7 terminal shell.

## Outcome

Outcome: READY_FOR_REVIEW

## What Was Done

- Reworked `launchpad config init` into a guided setup flow with clearer prompt
  copy, a final configuration summary, and explicit next-step guidance after
  writing the user config.
- Reworked `launchpad doctor` into grouped `Local Setup` and `Cluster Access`
  panels with actionable per-check follow-up copy, summary counts, and a
  branded all-pass success path only.
- Updated setup and diagnostics docs plus expanded tests to cover the guided
  setup flow, actionable config-init errors, grouped doctor output, and the
  success branding path.

## Files Changed

- docs/guides/getting-started.md
- docs/reference/commands/doctor.md
- docs/reference/configuration.md
- src/launchpad_cli/cli/config_cmd.py
- src/launchpad_cli/cli/doctor.py
- tests/test_config.py
- tests/test_operator_commands.py

## Verification Results

- `uv run pytest tests/test_config.py tests/test_cli.py` -> passed (14 passed)
- `uv run pytest` -> passed (117 passed)

## Observations

- I kept `launchpad config init` compatible with non-interactive automation:
  the new guidance is in the human-readable output, but the command still
  accepts explicit flags and writes the same user config file shape.
