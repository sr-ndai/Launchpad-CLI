# Builder Session - 2026-03-15 1228

## Task

`10.1` - Nastran env exports and live running-job metrics

## Actions Taken

- Added `solvers.nastran.environment` and `remote_binaries.sstat` to the
  config model and docs surface.
- Threaded configured Nastran environment variables into submit-script
  generation through the adapter and shared SLURM script builder.
- Added shared `sstat` command building, parsing, and query helpers, then
  merged live runtime metrics into human-readable `status` output.
- Updated `doctor` so missing or degraded `sacct` / `sstat` is warning-grade
  degraded observability rather than a hard blocker when core scheduler access
  still works.
- Updated docs and regression coverage for the new environment-export and
  live-metrics behavior.

## Verification

- `uv run pytest tests/test_solver_adapters.py tests/test_config.py tests/test_slurm.py tests/test_status.py tests/test_display.py tests/test_operator_commands.py`
- `uv run pytest`

## Outcome

Outcome: READY_FOR_REVIEW
