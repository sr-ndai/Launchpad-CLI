# Review: 8.1

**Verdict:** REVISION_NEEDED

## Summary
The task lands the intended manifest-first architecture: solver log catalogs are
captured at submit time, `launchpad-manifest.json` is written remotely, and
submit/status now surface manifest-backed task references. Verification is
green, but the task-reference alias format does not match the Phase 8 contract
that was queued and approved for Builder work.

## Issues
1. The implementation hard-codes aliases as `t01`, `t02`, ... with a minimum
   width of 2 instead of the approved plain zero-padded aliases like `001`,
   `002`, ... with a minimum width of 3. This shows up in the manifest builder
   at [src/launchpad_cli/core/job_manifest.py](/Users/srogachev/Projects/NEW/Launchpad-CLI/src/launchpad_cli/core/job_manifest.py#L75),
   and the wrong contract is then locked into the CLI/tests at
   [tests/test_submit.py](/Users/srogachev/Projects/NEW/Launchpad-CLI/tests/test_submit.py#L52)
   and [tests/test_status.py](/Users/srogachev/Projects/NEW/Launchpad-CLI/tests/test_status.py#L325).

## Required Changes
1. Change the generated alias format to the approved plain numeric,
   zero-padded form with minimum width 3, then update the touched submit/status
   surfaces and tests to match that contract consistently.
